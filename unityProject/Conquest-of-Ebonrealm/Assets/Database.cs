using UnityEngine;
using Mono.Data.Sqlite;
using System.Data;
using System;

//public class DatabaseScript : MonoBehaviour
//{
  // public void readdb()
   //{ 
        //connect database
     //   public void CreateDB()
       // {
         //   using (var connection = new SqliteConnection(Database))
           // {
             //   connection.Open();

                //object ("command") allows database control
               // using (var command = connection.CreateCommand())
                //{
                    //create tables
                    //creates player table, with two fields (health and damage)
                  //  command.CommandText = "CREATE TABLE IF NOT EXISTS player (health INT(20), damage INT(2));";
                    //command.ExecuteNonQuery();
                    //command.CommandText = "CREATE TABLE IF NOT EXISTS enemy (healht INT(4), damage INT(1));";
                    //command.ExecuteNonQuery();
                //}

//                connection.Close();
  //          }
    //    }
  // }
        
//}
